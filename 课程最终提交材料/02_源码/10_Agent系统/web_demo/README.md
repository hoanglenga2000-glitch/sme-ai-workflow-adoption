# Streamlit Demo

Run after installing `requirements.txt`:

```bash
streamlit run 10_Agent系统/web_demo/streamlit_app.py
```

The demo is intentionally thin: it calls the same agent tools used by tests and the API, so presentation cannot bypass evidence checks.
