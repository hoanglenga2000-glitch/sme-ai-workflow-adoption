# Security and Disclosure Policy

Do not commit `.env`, SSH keys, tokens, passwords, `.joblib`, `.pkl`, private questionnaire/interview records, or private manuscript packages.

Public releases should include only source manifests, reproducible scripts, lightweight tables, and public figures.
